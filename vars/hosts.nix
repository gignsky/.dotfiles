# Per-host active status for roll-flow and other tooling.
# Set `active = false` to exclude a host from consideration
# (e.g. while it's offline or being rebuilt).
{
  merlin = true;
  wsl = true;
  ganoslal = false;
  spacedock = false;
}
