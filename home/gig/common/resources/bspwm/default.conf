#!/bin/bash
# Default: Single monitor fallback configuration
# 
# Used for unknown hosts or when host-specific config is not available
#
# Desktop Distribution:
#   Single monitor: I II III IV V VI VII VIII IX X (10 desktops)

echo "Loading default single-monitor configuration..."

# Standard single monitor setup (10 desktops)
bspc monitor -d I II III IV V VI VII VIII IX X

echo "Default bspwm setup complete: Single monitor (I-X)"
